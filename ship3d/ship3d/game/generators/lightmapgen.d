module game.generators.lightmapgen;

import std.random;

import game.units;
import game.renderer.light;
import game.topology.room;

void generateLights(R,D)(auto ref R rnd, Room[] rooms)
{
    Light[][Room] roomLights;
    foreach(r; rooms[])
    {
    }
}